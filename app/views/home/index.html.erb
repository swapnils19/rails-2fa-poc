<div class="container mx-auto px-4 py-8">
  <div class="max-w-2xl mx-auto">
    <h1 class="text-3xl font-bold text-gray-800 mb-8">Rails 2FA POC</h1>

    <% if user_signed_in? %>
      <div class="bg-white rounded-lg shadow-md p-6 mb-6">
        <h2 class="text-xl font-semibold text-gray-800 mb-4">Welcome, <%= current_user.email %>!</h2>

        <div class="mb-6">
          <h3 class="text-lg font-medium text-gray-700 mb-3">Two-Factor Authentication Status</h3>

          <% if current_user.otp_enabled? %>
            <div class="flex items-center justify-between p-4 bg-green-50 border border-green-200 rounded-lg">
              <div class="flex items-center">
                <svg class="w-5 h-5 text-green-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                </svg>
                <span class="text-green-800 font-medium">2FA is enabled</span>
              </div>
              <%= link_to "Disable 2FA",
                  disable_two_factor_path,
                  data: {
                    "turbo-method": :delete,
                    confirm: "Are you sure you want to disable 2FA?"
                  },
                  class: "text-red-600 hover:text-red-800 font-medium" %>
            </div>
          <% else %>
            <div class="flex items-center justify-between p-4 bg-yellow-50 border border-yellow-200 rounded-lg">
              <div class="flex items-center">
                <svg class="w-5 h-5 text-yellow-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
                </svg>
                <span class="text-yellow-800 font-medium">2FA is not enabled</span>
              </div>
              <%= link_to "Setup 2FA",
                  setup_two_factor_path,
                  class: "bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700 font-medium" %>
            </div>
          <% end %>
        </div>

        <div class="border-t pt-4">
          <%= form_with url: destroy_user_session_path, method: :delete, local: true, class: "inline" do |form| %>
            <%= form.submit "Sign out",
                class: "text-gray-600 hover:text-gray-800 bg-transparent border-0 p-0 cursor-pointer underline" %>
          <% end %>
        </div>
      </div>
    <% else %>
      <div class="bg-white rounded-lg shadow-md p-6 text-center">
        <h2 class="text-xl font-semibold text-gray-800 mb-4">Welcome to Rails 2FA POC</h2>
        <p class="text-gray-600 mb-6">This is a proof of concept for implementing Two-Factor Authentication using Devise and ROTP.</p>

        <div class="space-x-4">
          <%= link_to "Sign In", new_user_session_path,
              class: "bg-blue-600 text-white px-6 py-2 rounded-md hover:bg-blue-700 font-medium" %>
          <%= link_to "Sign Up", new_user_registration_path,
              class: "bg-green-600 text-white px-6 py-2 rounded-md hover:bg-green-700 font-medium" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
